## Logo 字体说明

当前 Logo 使用的字体文件为：`ZhiMangXing-Regular.ttf`（志莽行书）。

### 文件位置

字体位于 `frontend/public/fonts/ZhiMangXing-Regular.ttf`，由 `frontend/src/index.css` 中的 `@font-face` 加载并应用到 `.brand-logo`。

### 建议

- 若后续更换字体，建议优先使用 `woff2`（体积更小、加载更快）。
- 字体请确保可商用/开源授权，避免后续版权风险。
